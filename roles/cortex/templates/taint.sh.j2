{% macro label() -%}
{% for host in groups['kube-storage'] %} kubectl label nodes {{ host }}  dedicated={{ cortex_taint }} ; {% endfor %}
{% endmacro -%}

{% macro taint() -%}
{% for host in groups['kube-storage'] %} kubectl taint nodes {{ host }} dedicated={{ cortex_taint }}:NoSchedule ;  {% endfor %}
{% endmacro -%}

{{ label() }} 
{{ taint() }}
